# C++ Module 09

STL exercises implemented in C++98:

- `ex00`: Bitcoin exchange lookup with `std::map`
- `ex01`: Reverse Polish notation evaluator with `std::stack` backed by `std::list`
- `ex02`: Ford-Johnson merge-insertion sort with `std::vector` and `std::deque`

Build each exercise from its own directory with `make`.
